# Edge --> AWS IoT Core

## Covers:
    * MQTT concept (brief)
    * Topic design:

    ```Bash
    indusstream/v3/telemetry
    ```

    * Payload structure
    * TLS + certificates (high level only)
## Include screenshots:
    * AWS IoT Core → MQTT test client
    * Live messages arriving
    * Terminal output from:

    ```Bash
        python3 -m src.edge_gateway
    ```